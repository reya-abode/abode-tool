# Turns on the Claude classification and narrative passes.
# Without it the app falls back to keyword parsing and template prose.
# Keep the real key in .env.local, which is never committed.
ANTHROPIC_API_KEY=
